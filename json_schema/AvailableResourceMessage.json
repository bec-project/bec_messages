{
  "description": "Message for available resources such as scans, data processing plugins etc\n\nArgs:\n    resource (dict, list[dict]): Resource description\n    metadata (dict, optional): Metadata. Defaults to None.",
  "properties": {
    "metadata": {
      "additionalProperties": true,
      "title": "Metadata",
      "type": "object"
    },
    "resource": {
      "anyOf": [
        {
          "additionalProperties": true,
          "type": "object"
        },
        {
          "items": {
            "additionalProperties": true,
            "type": "object"
          },
          "type": "array"
        }
      ],
      "title": "Resource"
    },
    "bec_codec": {
      "$ref": "BecCodecInfo.json",
      "readOnly": true
    }
  },
  "required": [
    "resource",
    "bec_codec"
  ],
  "title": "AvailableResourceMessage",
  "type": "object"
}
