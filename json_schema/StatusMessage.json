{
  "$defs": {
    "BECStatus": {
      "description": "BEC status enum",
      "enum": [
        2,
        1,
        0,
        -1
      ],
      "title": "BECStatus",
      "type": "integer"
    },
    "BecCodecInfo": {
      "properties": {
        "type_name": {
          "title": "Type Name",
          "type": "string"
        }
      },
      "required": [
        "type_name"
      ],
      "title": "BecCodecInfo",
      "type": "object"
    },
    "ServiceInfo": {
      "properties": {
        "user": {
          "title": "User",
          "type": "string"
        },
        "hostname": {
          "title": "Hostname",
          "type": "string"
        },
        "timestamp": {
          "title": "Timestamp",
          "type": "number"
        },
        "versions": {
          "$ref": "#/$defs/ServiceVersions"
        },
        "bec_codec": {
          "$ref": "#/$defs/BecCodecInfo",
          "readOnly": true
        }
      },
      "required": [
        "user",
        "hostname",
        "bec_codec"
      ],
      "title": "ServiceInfo",
      "type": "object"
    },
    "ServiceVersions": {
      "properties": {
        "bec_lib": {
          "title": "Bec Lib",
          "type": "string"
        },
        "bec_server": {
          "title": "Bec Server",
          "type": "string"
        },
        "bec_ipython_client": {
          "title": "Bec Ipython Client",
          "type": "string"
        },
        "bec_widgets": {
          "title": "Bec Widgets",
          "type": "string"
        },
        "bec_codec": {
          "$ref": "#/$defs/BecCodecInfo",
          "readOnly": true
        }
      },
      "required": [
        "bec_lib",
        "bec_server",
        "bec_ipython_client",
        "bec_widgets",
        "bec_codec"
      ],
      "title": "ServiceVersions",
      "type": "object"
    }
  },
  "description": "Status message\n\nArgs:\n    name (str): Name of the status.\n    status (BECStatus): Value of the BECStatus enum (RUNNING = 2,  BUSY = 1, IDLE = 0, ERROR = -1).\n    info (ServiceInfo | dict): Status info.\n    metadata (dict, optional): Additional metadata.",
  "properties": {
    "metadata": {
      "additionalProperties": true,
      "title": "Metadata",
      "type": "object"
    },
    "name": {
      "title": "Name",
      "type": "string"
    },
    "status": {
      "$ref": "#/$defs/BECStatus"
    },
    "info": {
      "anyOf": [
        {
          "$ref": "#/$defs/ServiceInfo"
        },
        {
          "additionalProperties": true,
          "type": "object"
        }
      ],
      "title": "Info"
    },
    "bec_codec": {
      "$ref": "#/$defs/BecCodecInfo",
      "readOnly": true
    }
  },
  "required": [
    "name",
    "status",
    "info",
    "bec_codec"
  ],
  "title": "StatusMessage",
  "type": "object"
}
