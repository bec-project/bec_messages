{
  "description": "Message for credentials\n\nArgs:\n    credentials (dict): Credentials\n    metadata (dict, optional): Metadata. Defaults to None.",
  "properties": {
    "metadata": {
      "additionalProperties": true,
      "title": "Metadata",
      "type": "object"
    },
    "credentials": {
      "additionalProperties": true,
      "title": "Credentials",
      "type": "object"
    },
    "bec_codec": {
      "$ref": "BecCodecInfo.json",
      "readOnly": true
    }
  },
  "required": [
    "credentials",
    "bec_codec"
  ],
  "title": "CredentialsMessage",
  "type": "object"
}
