{
  "description": "Message for GUI configuration\n\nArgs:\n    config (dict): GUI configuration, check widgets for more details\n    metadata (dict, optional): Metadata. Defaults to None.",
  "properties": {
    "metadata": {
      "additionalProperties": true,
      "title": "Metadata",
      "type": "object"
    },
    "config": {
      "additionalProperties": true,
      "title": "Config",
      "type": "object"
    },
    "bec_codec": {
      "$ref": "BecCodecInfo.json",
      "readOnly": true
    }
  },
  "required": [
    "config",
    "bec_codec"
  ],
  "title": "GUIConfigMessage",
  "type": "object"
}
