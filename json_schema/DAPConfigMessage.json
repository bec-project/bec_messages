{
  "description": "Message for DAP configuration\n\nArgs:\n    config (dict): DAP configuration dictionary\n    metadata (dict, optional): Metadata. Defaults to None.",
  "properties": {
    "metadata": {
      "additionalProperties": true,
      "title": "Metadata",
      "type": "object"
    },
    "config": {
      "additionalProperties": true,
      "title": "Config",
      "type": "object"
    },
    "bec_codec": {
      "$ref": "BecCodecInfo.json",
      "readOnly": true
    }
  },
  "required": [
    "config",
    "bec_codec"
  ],
  "title": "DAPConfigMessage",
  "type": "object"
}
